"""scripts.lib — shared helpers for catalog ingestion / lookup.

penrose_signal: neutral
penrose_dimension: provider_neutrality

This package houses operator-context-driven lookup primitives that implement
the Modular Replication via Input Substitution doctrine. The SAME helpers
filter catalogs for any operator (Carmen Beach / Tulum / Tamarindo / Canggu /
Bali) — only the `operator_context.tags` input differs.
"""
